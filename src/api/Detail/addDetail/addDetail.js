import { prisma } from "../../../../generated/prisma-client";
import axios from "axios";

// 갱신주기 설정 필요

export default {
  Mutation: {
    addDetail: async (_, __) => {
      return true;
    }
  }
};
