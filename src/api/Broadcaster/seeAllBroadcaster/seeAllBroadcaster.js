import { prisma } from "../../../../generated/prisma";

/**
 * ================================================================
 * 브로드캐스터 전체 조회
 * ================================================================
 * 수정예정
 * ================================================================
 */

export default {
  Query: {
    seeAllBroadcaster: async (_, __) => {
      return await prisma.broadcasters();
    }
  }
};
